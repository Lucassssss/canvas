{
  "name": "joii",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:desktop": "turbo run dev --filter=electron",
    "build": "turbo run build",
    "build:desktop": "turbo run build --filter=electron",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "desktop": "cd apps/electron && bun run start",
    "desktop:dev": "cd apps/electron && bun run dev",
    "desktop:build": "cd apps/electron && bun run build",
    "desktop:dist": "cd apps/electron && bun run dist",
    "deploy": "bash server_config/scripts/deploy.sh full",
    "deploy:frontend": "bash server_config/scripts/deploy.sh frontend",
    "deploy:backend": "bash server_config/scripts/deploy.sh backend",
    "seo:push": "bash server_config/scripts/baidu-push.sh"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^1.1.15",
    "turbo": "^2.8.21"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.10",
  "dependencies": {
    "@langchain/langgraph": "^1.2.1",
    "@radix-ui/react-scroll-area": "^1.2.10",
    "deepagents": "^1.8.1",
    "lucide-react": "^0.577.0",
    "nanoid": "^5.1.7",
    "react": "^19.2.4",
    "react-markdown": "^10.1.0",
    "react-syntax-highlighter": "^16.1.1",
    "remark-gfm": "^4.0.1",
    "zustand": "^5.0.11"
  }
}
