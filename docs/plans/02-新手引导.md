# 新手引导开发计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 Joii 画布应用创建完整的首次用户引导流程，让新用户在首次使用时能够快速了解核心功能并完成首次作品创作。

**Architecture:** 使用 React Context 管理引导状态，使用 localStorage 持久化引导完成状态。引导流程包括：欢迎页 → 功能介绍 Tooltips → 首次换装教程 → 首次导出引导。

**Tech Stack:** Next.js 16, React Context, TailwindCSS, localStorage

---

## Task 1: 创建引导状态管理 Context

**Files:**
- Create: `apps/web/src/features/onboarding/OnboardingContext.tsx`

**Step 1: 创建 OnboardingContext**

```tsx
'use client'

import React, { createContext, useContext, useState, useCallback, useEffect } from 'react'

interface OnboardingState {
  isCompleted: boolean
  currentStep: number
  totalSteps: number
  hasSeenTryOn: boolean
  hasCreatedFirstDesign: boolean
  hasExportedFirstImage: boolean
}

interface OnboardingContextValue {
  state: OnboardingState
  startOnboarding: () => void
  completeStep: (step: number) => void
  skipOnboarding: () => void
  markTryOnSeen: () => void
  markFirstDesignCreated: () => void
  markFirstExported: () => void
  resetOnboarding: () => void
}

const STORAGE_KEY = 'joii_onboarding_completed'
const TOTAL_STEPS = 5

const defaultState: OnboardingState = {
  isCompleted: false,
  currentStep: 0,
  totalSteps: TOTAL_STEPS,
  hasSeenTryOn: false,
  hasCreatedFirstDesign: false,
  hasExportedFirstImage: false,
}

const OnboardingContext = createContext<OnboardingContextValue | null>(null)

export function OnboardingProvider({ children }: { children: React.ReactNode }) {
  const [state, setState] = useState<OnboardingState>(defaultState)

  // 从 localStorage 恢复状态
  useEffect(() => {
    const stored = localStorage.getItem(STORAGE_KEY)
    if (stored) {
      try {
        const parsed = JSON.parse(stored)
        setState({ ...defaultState, ...parsed, isCompleted: true })
      } catch (e) {
        console.error('Failed to parse onboarding state:', e)
      }
    }
  }, [])

  // 持久化状态
  useEffect(() => {
    if (state.isCompleted) {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(state))
    }
  }, [state])

  const startOnboarding = useCallback(() => {
    setState((prev) => ({ ...prev, currentStep: 1 }))
  }, [])

  const completeStep = useCallback((step: number) => {
    setState((prev) => {
      const nextStep = step + 1
      if (nextStep > TOTAL_STEPS) {
        return { ...prev, currentStep: nextStep, isCompleted: true }
      }
      return { ...prev, currentStep: nextStep }
    })
  }, [])

  const skipOnboarding = useCallback(() => {
    setState((prev) => ({ ...prev, isCompleted: true }))
  }, [])

  const markTryOnSeen = useCallback(() => {
    setState((prev) => ({ ...prev, hasSeenTryOn: true }))
  }, [])

  const markFirstDesignCreated = useCallback(() => {
    setState((prev) => ({ ...prev, hasCreatedFirstDesign: true }))
  }, [])

  const markFirstExported = useCallback(() => {
    setState((prev) => ({ ...prev, hasExportedFirstImage: true }))
  }, [])

  const resetOnboarding = useCallback(() => {
    localStorage.removeItem(STORAGE_KEY)
    setState(defaultState)
  }, [])

  return (
    <OnboardingContext.Provider
      value={{
        state,
        startOnboarding,
        completeStep,
        skipOnboarding,
        markTryOnSeen,
        markFirstDesignCreated,
        markFirstExported,
        resetOnboarding,
      }}
    >
      {children}
    </OnboardingContext.Provider>
  )
}

export function useOnboarding() {
  const context = useContext(OnboardingContext)
  if (!context) {
    throw new Error('useOnboarding must be used within OnboardingProvider')
  }
  return context
}
```

---

## Task 2: 创建引导步骤配置

**Files:**
- Create: `apps/web/src/features/onboarding/steps.ts`

**Step 1: 定义引导步骤配置**

```tsx
export interface OnboardingStep {
  id: string
  title: string
  description: string
  targetSelector?: string
  position: 'top' | 'bottom' | 'left' | 'right' | 'center'
  action?: {
    type: 'click' | 'drag' | 'input'
    label: string
  }
}

export const ONBOARDING_STEPS: OnboardingStep[] = [
  {
    id: 'welcome',
    title: '欢迎使用 Joii',
    description: '让我们用 1 分钟快速了解 Joii 的核心功能，开启您的电商设计之旅。',
    position: 'center',
    action: { type: 'click', label: '开始引导' },
  },
  {
    id: 'toolbar',
    title: '工具栏',
    description: '这里是工具栏，您可以找到选择、画笔、形状、图片上传等基础工具。',
    targetSelector: '[data-onboarding="toolbar"]',
    position: 'right',
  },
  {
    id: 'ai-tryon',
    title: 'AI 换装组件',
    description: '拖入一张模特图和服装图，点击生成按钮即可完成 AI 智能换装。',
    targetSelector: '[data-onboarding="ai-tryon"]',
    position: 'left',
    action: { type: 'click', label: '我知道了' },
  },
  {
    id: 'chat-assistant',
    title: 'AI 助手',
    description: '您可以随时通过 AI 助手获取帮助，或让 AI 帮您生成图片、编辑画布。',
    targetSelector: '[data-onboarding="chat-assistant"]',
    position: 'left',
  },
  {
    id: 'export',
    title: '导出作品',
    description: '完成设计后，点击右上角的导出按钮即可导出您的作品。',
    targetSelector: '[data-onboarding="export"]',
    position: 'bottom',
    action: { type: 'click', label: '完成引导' },
  },
]
```

---

## Task 3: 创建引导遮罩和高亮组件

**Files:**
- Create: `apps/web/src/features/onboarding/OnboardingOverlay.tsx`

**Step 1: 创建 OnboardingOverlay 组件**

```tsx
'use client'

import React, { useEffect, useState, useRef } from 'react'
import { X, ChevronRight, Sparkles } from 'lucide-react'
import { useOnboarding } from './OnboardingContext'
import { ONBOARDING_STEPS, OnboardingStep } from './steps'

interface OnboardingOverlayProps {
  isActive: boolean
}

export function OnboardingOverlay({ isActive }: OnboardingOverlayProps) {
  const { state, completeStep, skipOnboarding } = useOnboarding()
  const [currentStepData, setCurrentStepData] = useState<OnboardingStep | null>(null)
  const [targetRect, setTargetRect] = useState<DOMRect | null>(null)
  const overlayRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    if (!isActive || state.currentStep === 0) {
      setCurrentStepData(null)
      setTargetRect(null)
      return
    }

    const stepData = ONBOARDING_STEPS[state.currentStep - 1]
    if (!stepData) {
      setCurrentStepData(null)
      return
    }

    setCurrentStepData(stepData)

    if (stepData.targetSelector) {
      const element = document.querySelector(stepData.targetSelector)
      if (element) {
        setTargetRect(element.getBoundingClientRect())
      }
    }
  }, [isActive, state.currentStep])

  if (!isActive || !currentStepData) return null

  const handleAction = () => {
    completeStep(state.currentStep)
  }

  return (
    <div
      ref={overlayRef}
      className="fixed inset-0 z-[9999] pointer-events-none"
    >
      {/* 遮罩层 */}
      <div className="absolute inset-0 bg-black/50 pointer-events-auto" />

      {/* 高亮区域 */}
      {targetRect && (
        <div
          className="absolute pointer-events-none"
          style={{
            top: targetRect.top - 8,
            left: targetRect.left - 8,
            width: targetRect.width + 16,
            height: targetRect.height + 16,
            boxShadow: '0 0 0 9999px rgba(0, 0, 0, 0.5)',
            borderRadius: 8,
          }}
        />
      )}

      {/* 引导卡片 */}
      <div className="absolute p-6 bg-white rounded-xl shadow-2xl max-w-sm pointer-events-auto"
        style={
          targetRect
            ? getCardPosition(targetRect, currentStepData.position)
            : { top: '50%', left: '50%', transform: 'translate(-50%, -50%)' }
        }
      >
        <button
          onClick={skipOnboarding}
          className="absolute top-3 right-3 p-1 text-gray-400 hover:text-gray-600"
        >
          <X size={16} />
        </button>

        <div className="flex items-center gap-2 mb-3">
          <Sparkles className="w-4 h-4 text-blue-500" />
          <span className="text-xs font-medium text-blue-500">
            引导 {state.currentStep} / {state.totalSteps}
          </span>
        </div>

        <h3 className="font-sans-zh font-semibold text-lg mb-2">
          {currentStepData.title}
        </h3>
        <p className="text-sm text-gray-600 mb-4">
          {currentStepData.description}
        </p>

        {currentStepData.action && (
          <button
            onClick={handleAction}
            className="w-full py-2 bg-blue-500 text-white text-sm font-medium rounded-lg hover:bg-blue-600 transition-colors flex items-center justify-center gap-2"
          >
            {currentStepData.action.label}
            <ChevronRight className="w-4 h-4" />
          </button>
        )}
      </div>
    </div>
  )
}

function getCardPosition(rect: DOMRect, position: string) {
  const cardWidth = 320
  const cardHeight = 200
  const gap = 16

  switch (position) {
    case 'top':
      return {
        bottom: window.innerHeight - rect.top + gap,
        left: rect.left + rect.width / 2 - cardWidth / 2,
      }
    case 'bottom':
      return {
        top: rect.bottom + gap,
        left: rect.left + rect.width / 2 - cardWidth / 2,
      }
    case 'left':
      return {
        top: rect.top + rect.height / 2 - cardHeight / 2,
        right: window.innerWidth - rect.left + gap,
      }
    case 'right':
      return {
        top: rect.top + rect.height / 2 - cardHeight / 2,
        left: rect.right + gap,
      }
    default:
      return {
        top: '50%',
        left: '50%',
        transform: 'translate(-50%, -50%)',
      }
  }
}
```

---

## Task 4: 创建首次换装教程组件

**Files:**
- Create: `apps/web/src/features/onboarding/FirstTryOnTutorial.tsx`

**Step 1: 创建首次换装教程组件**

```tsx
'use client'

import React, { useState } from 'react'
import { Play, Upload, Sparkles, Check } from 'lucide-react'
import { useOnboarding } from './OnboardingContext'

interface FirstTryOnTutorialProps {
  onComplete: () => void
}

export function FirstTryOnTutorial({ onComplete }: FirstTryOnTutorialProps) {
  const { markTryOnSeen, markFirstDesignCreated } = useOnboarding()
  const [step, setStep] = useState<'model' | 'clothing' | 'generating' | 'result'>('model')

  const handleModelUploaded = () => {
    setStep('clothing')
  }

  const handleClothingUploaded = () => {
    setStep('generating')
    setTimeout(() => {
      setStep('result')
      markTryOnSeen()
      markFirstDesignCreated()
      onComplete()
    }, 2000)
  }

  return (
    <div className="fixed inset-0 z-[9998] bg-white/95 flex items-center justify-center">
      <div className="max-w-lg w-full mx-4">
        <div className="text-center mb-8">
          <div className="inline-flex items-center justify-center w-16 h-16 bg-blue-100 rounded-full mb-4">
            <Sparkles className="w-8 h-8 text-blue-500" />
          </div>
          <h2 className="font-sans-zh text-2xl font-semibold mb-2">
            首次 AI 换装体验
          </h2>
          <p className="text-gray-600">
            让我们一起完成您的第一次 AI 换装设计
          </p>
        </div>

        {/* 步骤指示器 */}
        <div className="flex justify-center gap-2 mb-8">
          {['model', 'clothing', 'generating', 'result'].map((s, i) => (
            <div
              key={s}
              className={`w-2 h-2 rounded-full transition-colors ${
                step === s ? 'bg-blue-500' :
                ['model', 'clothing', 'generating', 'result'].indexOf(step) > i
                  ? 'bg-green-500' : 'bg-gray-300'
              }`}
            />
          ))}
        </div>

        {/* 教程内容 */}
        <div className="bg-gray-50 rounded-2xl p-6">
          {step === 'model' && (
            <div className="text-center">
              <p className="mb-4 text-gray-700">第一步：上传一张模特图片</p>
              <button
                onClick={handleModelUploaded}
                className="inline-flex items-center gap-2 px-6 py-3 bg-white border-2 border-dashed border-gray-300 rounded-xl hover:border-blue-500 hover:text-blue-500 transition-colors"
              >
                <Upload className="w-5 h-5" />
                <span>上传模特图</span>
              </button>
            </div>
          )}

          {step === 'clothing' && (
            <div className="text-center">
              <div className="mb-4 flex justify-center">
                <div className="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center">
                  <Check className="w-8 h-8 text-green-500" />
                </div>
              </div>
              <p className="mb-4 text-gray-700">模特已就绪！第二步：上传服装图片</p>
              <button
                onClick={handleClothingUploaded}
                className="inline-flex items-center gap-2 px-6 py-3 bg-white border-2 border-dashed border-gray-300 rounded-xl hover:border-blue-500 hover:text-blue-500 transition-colors"
              >
                <Upload className="w-5 h-5" />
                <span>上传服装图</span>
              </button>
            </div>
          )}

          {step === 'generating' && (
            <div className="text-center">
              <div className="mb-4 flex justify-center">
                <Sparkles className="w-12 h-12 text-blue-500 animate-spin" />
              </div>
              <p className="text-gray-700">AI 正在生成中...</p>
            </div>
          )}

          {step === 'result' && (
            <div className="text-center">
              <div className="mb-4 flex justify-center">
                <div className="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center">
                  <Check className="w-8 h-8 text-green-500" />
                </div>
              </div>
              <p className="text-lg font-medium text-gray-900 mb-2">恭喜完成首次换装！</p>
              <p className="text-gray-600">您已掌握 Joii 的核心功能</p>
            </div>
          )}
        </div>
      </div>
    </div>
  )
}
```

---

## Task 5: 在 Canvas 页面集成引导组件

**Files:**
- Modify: `apps/web/src/app/canvas/page.tsx`
- Modify: `apps/web/src/app/canvas/Canvas.tsx`
- Modify: `apps/web/src/app/layout.tsx`

**Step 1: 在 layout.tsx 添加 OnboardingProvider**

**Modify:** `apps/web/src/app/layout.tsx`

```tsx
import { OnboardingProvider } from '@/features/onboarding/OnboardingContext'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="zh-CN">
      <body>
        <OnboardingProvider>
          {children}
        </OnboardingProvider>
      </body>
    </html>
  )
}
```

**Step 2: 在 Canvas 页面添加 data-onboarding 属性**

**Modify:** `apps/web/src/app/canvas/components/Toolbar.tsx`

```tsx
<div data-onboarding="toolbar" className="flex items-center gap-2">
  {/* 现有工具按钮 */}
</div>
```

**Modify:** `apps/web/src/app/canvas/shapes/AICombinationComponent.tsx`

```tsx
<div data-onboarding="ai-tryon" className="w-full h-full ...">
  {/* AI 换装组件内容 */}
</div>
```

**Step 3: 在 Canvas 页面集成引导**

**Modify:** `apps/web/src/app/canvas/page.tsx`

```tsx
'use client'

import { Canvas } from './Canvas'
import { OnboardingOverlay } from '@/features/onboarding/OnboardingOverlay'
import { useOnboarding } from '@/features/onboarding/OnboardingContext'
import { useEffect } from 'react'

export default function CanvasPage() {
  const { state, startOnboarding } = useOnboarding()

  useEffect(() => {
    if (!state.isCompleted) {
      const timer = setTimeout(() => {
        startOnboarding()
      }, 1000)
      return () => clearTimeout(timer)
    }
  }, [state.isCompleted, startOnboarding])

  return (
    <>
      <Canvas />
      <OnboardingOverlay isActive={!state.isCompleted && state.currentStep > 0} />
    </>
  )
}
```

---

## Task 6: 创建引导 API 接口

**Files:**
- Create: `apps/api/src/routes/onboarding.ts`
- Modify: `apps/api/src/routes/index.ts`

**Step 1: 创建 onboarding 路由**

**Modify:** `apps/api/src/routes/index.ts`

```ts
import { Router } from 'express'
import { onboardingRouter } from './onboarding'

export function setupRoutes(app: Router) {
  // ... existing routes
  app.use('/api/onboarding', onboardingRouter)
}
```

**Create:** `apps/api/src/routes/onboarding.ts`

```ts
import { Router, Request, Response } from 'express'

const router = Router()

interface OnboardingStats {
  completedTutorial: boolean
  firstDesignCreated: boolean
  firstExportCompleted: boolean
}

router.get('/stats', async (req: Request, res: Response) => {
  // 获取用户引导状态
  // TODO: 集成数据库查询
  const stats: OnboardingStats = {
    completedTutorial: false,
    firstDesignCreated: false,
    firstExportCompleted: false,
  }
  res.json({ success: true, stats })
})

router.post('/complete', async (req: Request, res: Response) => {
  const { step } = req.body
  // 记录引导完成状态
  // TODO: 集成数据库更新
  res.json({ success: true })
})

export { router as onboardingRouter }
```

---

## Task 7: 测试引导流程

**Step 1: 测试首次加载**

Run: `cd apps/web && bun run dev`
Expected: 首次访问 /canvas 页面时自动弹出引导

**Step 2: 测试引导步骤跳转**

Expected: 点击引导卡片按钮能正确跳转到下一步

**Step 3: 测试跳过功能**

Expected: 点击 X 按钮能跳过引导

**Step 4: 测试状态持久化**

Expected: 刷新页面后引导不再出现

---

## Task 8: Commit

```bash
git add apps/web/src/features/onboarding/
git add apps/api/src/routes/onboarding.ts
git add apps/web/src/app/canvas/page.tsx
git add apps/web/src/app/layout.tsx
git commit -m "feat(onboarding): add interactive onboarding flow for new users"
```
